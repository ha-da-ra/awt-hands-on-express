<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>Book List</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            padding: 20px;
        }

        .book-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .book-item {
            background-color: #ffffff;
            border: 2px solid #ccc;
            margin-bottom: 10px;
            padding: 20px;
            position: relative;
            transition: transform 0.2s;
        }

        .book-item:before {
            content: '';
            position: absolute;
            top: 0;
            bottom: 0;
            left: -10px;
            width: 20px;
            background: linear-gradient(to left, #ffffff, #f0e68c);
            border-top-left-radius: 8px;
            border-bottom-left-radius: 8px;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
        }


        .book-item h1 {
            font-size: 1.5em;
            color: #333;
            margin-bottom: 10px;
        }

        .book-item h2 {
            font-size: 1.2em;
            color: #555;
            margin-bottom: 20px;
        }

        .book-item p {
            color: #666;
            margin: 5px 0;
        }

        .book-item .rating {
            font-weight: bold;
            color: #28a745;
            margin-bottom: 10px;
        }

        .book-item .already-read {
            margin-top: 20px;
        }

        .book-item .already-read span {
            color: #007bff;
        }

        .add-book-link {
            display: inline-block;
            margin-bottom: 20px;
            text-decoration: none;
            color: #007bff;
            border: 1px solid #007bff;
            padding: 5px 10px;
            border-radius: 4px;
        }

        .add-book-link:hover {
            background-color: #007bff;
            color: #ffffff;
        }

        .book-list .rating {
            font-weight: bold;
            color: #dfbe3a;
        }
    </style>
</head>

<body>
    <h1>Book List</h1>
    <ul class="book-list">
        <%# Start Task 3 %>

        <% books.forEach(function(book) { %>
            <%- include("partials/book", {book: book})%>
        <% }); %>

        <%# End Task 3 %>
    </ul>
</body>

</html>